{
  "routes": [
    { "route": "/programs", "redirect": "/services", "statusCode": 301 }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/assets/*",
      "/images/*",
      "/share/*",
      "/*.xml",
      "/*.txt",
      "/*.ico",
      "/*.json"
    ]
  }
}
